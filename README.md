# fastapi-space-bridge
# Ask API

API مبني على FastAPI يستخدم أدوات Gradio Client للتفاعل مع نموذجين:

- DeepSeek
- Web Search

## التشغيل المحلي

```bash
pip install -r requirements.txt
uvicorn app:app --reload
